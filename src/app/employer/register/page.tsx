import RegisterForm from "./RegisterForm";

export const metadata = {
  title: "Employer sign up",
  description: "Create an employer account to post jobs on Guam Job Listings.",
};

export default function EmployerRegisterPage() {
  return <RegisterForm />;
}
